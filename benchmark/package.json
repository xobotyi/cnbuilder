{
  "private": true,
  "dependencies": {},
  "scripts": {
    "bench:common": "yarn && node ./src/common.js",
    "bench:dedupe": "yarn && node ./src/dedupe.js"
  },
  "devDependencies": {
    "benchmark": "^2.1.4",
    "classcat": "*",
    "classnames": "*",
    "clsx": "*",
    "cnbuilder": "latest"
  }
}
