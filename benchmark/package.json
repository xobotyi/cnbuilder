{
  "private": true,
  "dependencies": {},
  "scripts": {
    "bench:common": "yarn && node ./src/common.js",
    "bench:dedupe": "yarn && node ./src/dedupe.js",
    "bench:common:browser": "browserify ./src/common.js -o ./browser/common.js && open-cli ./browser/index.html"
  },
  "devDependencies": {
    "benchmark": "^2.1.4",
    "browserify": "*",
    "classcat": "*",
    "classnames": "*",
    "clsx": "*",
    "cnbuilder": "*",
    "open-cli": "^7.2.0"
  }
}
